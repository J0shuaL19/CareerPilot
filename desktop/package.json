{
  "name": "careerpilot-desktop",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:smoke": "tauri build --no-bundle --ci",
    "build:installer-smoke": "tauri build --bundles nsis --ci",
    "smoke:packaged": "pwsh -NoProfile -File scripts/smoke-packaged-app.ps1",
    "smoke:installer": "pwsh -NoProfile -File scripts/smoke-installer.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
